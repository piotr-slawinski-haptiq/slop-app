DATABASE_URL=postgres://postgres:postgres@localhost:5432/slop
ALLOWED_EMAIL_DOMAIN=haptiq.com
APP_URL=http://localhost:3000

# Use a long random value in production.
MAGIC_LINK_SECRET=replace-with-a-long-random-secret

# Optional role seeding (comma-separated).
ORDERER_EMAILS=orderer@haptiq.com

# Optional session/token tuning.
SESSION_COOKIE_NAME=slop_session
SESSION_DURATION_DAYS=30
MAGIC_LINK_DURATION_MINUTES=15

# Optional SMTP transport for sending real emails.
# If omitted, magic links are logged on the server and shown on the login page in dev.
SMTP_URL=
EMAIL_FROM=
